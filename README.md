# Debitoo Backend Challenge

This project is a REST API developed with Laravel and MySQL. It allows users to register clients and manage their debt cases. Each case follows a simple workflow through the `new`, `in_progress`, and `closed` statuses.
